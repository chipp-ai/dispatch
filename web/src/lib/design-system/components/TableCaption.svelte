<caption class="table-caption" {...$$restProps}>
  <slot />
</caption>

<style>
  .table-caption {
    margin-top: var(--space-4);
    font-size: var(--text-sm);
    color: var(--text-muted);
  }
</style>
